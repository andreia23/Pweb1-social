import { TelefonicoPipe } from './telefonico.pipe';

describe('TelefonicoPipe', () => {
  it('create an instance', () => {
    const pipe = new TelefonicoPipe();
    expect(pipe).toBeTruthy();
  });
});
